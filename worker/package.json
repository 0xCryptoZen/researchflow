{
  "name": "researchflow-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create researchflow",
    "db:push": "wrangler d1 execute researchflow --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240003.0",
    "wrangler": "^3.28.0"
  }
}
